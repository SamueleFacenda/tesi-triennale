\chapter{Introduzione}
\label{cha:introduction}

Il presente lavoro ha origine dalle difficoltà riscontrate nello sviluppo del visualizzatore 3dont \ref{subsec:visualizer},
che ha richiesto un'attenta ricerca tra il vasto numero di triple store esistenti per far fronte a un carico di lavoro
non comune, approfondito successivamente.
Questa tipologia di interazioni con il triple store può essere più generalmente descritta come un'analisi di Big Data e in questa tesi
si troverà un'evoluzione strutturata della ricerca sommaria inizialmente condotta, andando a plasmare un 
benchmark riproducibile attorno al carico di lavoro richiesto ed analizzando le prestazioni di otto triple store 
nella sua esecuzione.
Questi risultati permettono dunque di discutere dei vantaggi dell'utilizzo di un grafo di conoscenza come metodo di
rappresentazione di Big Data, metodo le cui prestazioni sono storicamente lo svantaggio maggiore, ma può presentare
delle potenzialità considerevoli rispetto ai formati di rappresentazione nativi.

\par Si noti che si è parlato di Big Data nonostante il volume di dati analizzato sia nell'ordine delle decine di milioni
di unità (triple), elevato ma assolutamente ordinario. Ciò potrebbe sembrare andar contro alla definizione intuitiva di Big
Data, ma in realtà per le caratteristiche qui analizzate questa formulazione è più che lecita. Prendiamo ad esempio la
popolare definizione delle tre V(TODO cite) (Volume, Velocità e Varietà), la terza è sicuramente una caratteristica dei
dati qui trattati: considerando il caso di 3Dont \ref{sec:3dont} trattiamo contemporaneamente informazioni geospaziali sotto
forma di nuvola di punti, informazioni sui materiali e la loro lavorazione, sulla struttura e topologia di un edificio, 
informazioni sensoristiche variabili nel tempo e non solo. 
Il fatto che i Big Data non siano definiti soltanto dal loro volume è consolidato nella comunità scientifica (TODO cite Kitchin, R. (2013), "Big data and human geography", Dialogues in Human Geography, 3(3), pp. 262–267),
anche se l'aggettivo "Big" non contribuisce certo a renderlo intuitivo.

\par
RDF (Resource Description Framework)~\cite{w3c-rdf11} è la specifica definita da W3C per la rappresentazione di ontologie e grafi
di conoscenze nell'ambito del web semantico, oggi in corso di revisione verso la versione 1.2~\cite{w3c-rdf12}. È uno standard essenziale con il potenziale per essere esteso,
come ad esempio succede con OWL (Web Ontology Language)~\cite{w3c-owl2}, una sua potente estensione utilizzata per definire
ontologie complete. Di base RDF è stato creato per la condivisione di risorse web, ognuna identificata da un IRI univoco, ha dato il via alla creazione di un esteso ecosistema di applicativi e librerie
software per l'interrogazione e la manipolazione di grafi RDF. Nella pratica, molti DBMS sono stati integrati o creati con
il supporto per interrogazioni SPARQL (SPARQL Protocol and RDF Query Language)~\cite{w3c-sparql11}, il protocollo e linguaggio standardizzato
sempre da W3C per interrogare basi di conoscenza nel web semantico.
\par
Anche se il semantic web non ha raggiunto l'adozione e l'impatto sperato al momento della creazione di questi
standard~\cite{bernerslee2001semantic},
rimane comunque alla base di uno dei più vasti e avanzati sistemi per la manipolazione di basi di conoscenza. Inoltre,
grazie alla sua estensibilità by-design esso permette facilmente di valicare i domini della rete per essere utilizzato
in ambienti fondamentalmente diversi rispetto all'originale idea del semantic web.
Tramite la creazione di ontologie specifiche per dei dati domini è possibile rappresentare qualsiasi tipo di dato
tramite modelli RDF, mantenendo la stessa espressività del formato di partenza e quindi la completezza.
I vantaggi con questo approccio sono molteplici, in primis si andrebbe a creare una rappresentazione estensibile per il
dominio preso in analisi, permettendo a chiunque abbia accesso all'ontologia di espandere le possibilità di rappresentazione
del modello senza compromettere l'interoperabilità con altri sistemi non aggiornati. Questo non è invece possibile quasi mai
con le rappresentazioni native solitamente utilizzate per i loro domini, focalizzate più sulle prestazioni che sull'estensibilità
(si veda il capitolo relativo a 3Dont).
Inoltre, con questo approccio, si può andare a sfruttare la potenza dei sistemi di ragionamento sui modelli utilizzati,
andando a definire regole per espandere la conoscenza nelle nostre rappresentazioni e potenzialmente anche ottenere un guadagno
dal punto di vista della memoria di massa utilizzata, se il motore che stiamo andando ad utilizzare supporta il ragionamento ``virtuale''
a tempo di interrogazione (come nel caso di graphdb~\cite{bishop2011owlim} o owlready2~\cite{lamy2017owlready}, approfonditi nel capitolo~\ref{cha:benchmark}).
Per finire, si renderebbe possibile l'accesso e l'analisi dei dati così organizzati attraverso le query SPARQL, le quali garantiscono
espressività e una capacità di interrogazione avanzata. Calcoli e analisi complesse che richiederebbero l'implementazione di algoritmi
ad-hoc possono essere completati con una sola query SPARQL, aumentando l'accessibilità e la facilità di fruizione dei dati.
\par
In questa tesi si andrà quindi ad analizzare la fattibilità tecnica dell'utilizzo di RDF per la rappresentazione di dati 
di dominio specifico, con una particolare focalizzazione sull'ambito dei big data, che se rappresentati come 
grafi di conoscenza superano le decine di milioni di triple.
Queste grandi quantità di dati pongono appunto un problema all'ecosistema utilizzato. Se il problema della rappresentazione è per
così dire ``risolto'' dalla grande espressività ed estensibilità by-design di RDF, quello delle prestazioni è di altra natura.
Se vogliamo utilizzare RDF come modello di persistenza per big data in modo generale dobbiamo fare i conti con dei pattern di accesso
fondamentalmente diversi da quelli tipici del semantic web. Si andrà spesso ad analizzare l'interezza del modello oppure
delle sottoporzioni per andare a condurre delle analisi su questi dati, o produrre delle rappresentazioni in formati alternativi
(grafici, ad esempio) per cui è necessario accedere in lettura a grandi porzioni di dati. Stiamo quindi parlando di query di tipo OLAP~\cite{codd1998olap,chaudhuri1997olap}
contro il tipico carico di lavoro OLTP del semantic web: la sua natura predispone la ricerca per singole risorse ed elementi collegati,
facilmente ottimizzabile con l'utilizzo di indici a vario livello. La maggior parte dei DBMS con supporto per SPARQL non sono quindi
ottimizzati per query che contengono scansioni su grandi porzioni del modello quanto sull'accesso casuale e sul
collegamento dei nodi~\cite{ali2022survey}.
Questa è appunto la sfida più grande all'utilizzo diffuso dell'approccio proposto, e in questa tesi si andranno quindi soprattutto
ad analizzare le prestazioni di un certo numero di motori di query SPARQL nell'esecuzione di carichi di lavoro specifici,
con lo scopo di valutarne la predisposizione all'utilizzo con big data di dominio diverso dal web semantico.
Nello specifico si andranno ad impiegare datasets e query utilizzate dal framework 3Dont~\cite{codiglione2024urban,codiglione2024heritage},
approfondito nel capitolo dedicato,
all'interno di vari motori la cui prerogativa principale è che siano eseguibili localmente. Questo è importante soprattutto per
poter confrontare le prestazioni dei vari motori nello stesso ambiente, ma anche perché se si volessero poi utilizzare come base
per l'accesso ai modelli RDF è importante che possano essere integrati all'interno degli applicativi che li utilizzerebbero.

\section{Stato dell'Arte}
\label{sec:prior_works}

L'ambiente di ricerca e sviluppo che negli anni si è creato intorno a RDF e al semantic web è ricco e pieno di spunti.
Vale quindi la pena analizzare alcuni lavori correlati a questa tesi per il contenuto e temi trattati come per l'approccio
alternativo allo standard.
Si riporta quindi una breve lista di ricerche e progetti che hanno usato RDF appunto come formato per l'organizzazione 
di dati, sfruttando le sue capacità aggregative, oltre che benchmark preesistenti, con una comparazione negli aspetti
qui proposti e nelle loro differenze.

\subsection{Progetti rilevanti}

Un progetto molto interessante è Linked Building Data~\cite{bassier2022lbd}, che aggrega dati da diverse sorgenti per monitorare
lo sviluppo dei cantieri. Parte da una rappresentazione RDF dei dati BIM e usa l'ontologia V4Design~\cite{bassier2020v4design}
come base di costruzione semantica. Integra inoltre GEOSPARQL~\cite{ogc-geosparql11} per la correlazione di dati geospaziali
insieme all'ontologia FOG~\cite{bonduel2019fog} per la rappresentazione dei punti.
Il punto chiave del progetto poi è il collegamento con la sensoristica esistente, è un problema comune (perlaltro affrontato 
anche da 3Dont) quello dell'integrazione di dati da sorgenti così disparate, mantendendo una coesione spaziotemporale:
basti pensare al gran numero di formati proprietari usati dai sensori sul mercato, le diverse proiezioni geografiche e
temporali (ad esempio i sensori GNSS spesso usano il tempo GPS, leggermente ma significativamente scostato da quello UTC).
Un aspetto interessante sicuramente il fatto che vengano usate molte ongologie già esistenti per la definizione dei dati,
rendendo ancora più semplice l'interoperabilità, anche se GEOSPARQL limita l'utilizzo ai soli triple store che lo supportano,
come GraphDB e Virtuoso.

\par Nell'ambito del progetto WaterFRAME inoltre è stato sperimentato l'utilizzo di LDES (Linked Data Event
Streams)~\cite{ldes-spec}, uno standard basato su RDF per la gestione di eventi sequenziali e serie di dati temporali.
L'utilizzo di Time Series Snippets inoltre permette di ridurre la quantità di dati da processare, andando a compattare
senza perdita di conoscenza le triple che descrivono una serie temporale in un solo valore letterale.
I dati, modellati con GEOSPARQL insieme a SSN/SOSA e QUDT, vengono poi caricati in tre motori diversi, Virtuoso, Oxigraph
e Postgres, per confrontarne il comportamento~\cite{abdelfatah2026water}. È un confronto vicino a quello condotto qui,
anche se limitato a un carico di lavoro di sole serie temporali.

\par Anche Brick~\cite{balaji2018brick} prova a risolvere il problema dell'integrazione sensoristica, diventato chiave per l'interpretazione
e l'ottimizzazione dei consumi energetici negli edifici. Usando appunto un'ontologia comune sarebbe possibile avere software
che funzionano per un vasto numero di edifici senza nessuna modifica, anche se la completezza nella rappresentazione
è una sfida complicata. Il risultato è un framework open source estensibile by-default. 
Sistemi come questi sono essenziali e condividono l'idea alla base del semantic web, evitano il problema del vendor lock-in
e in questi casi raggiungono anche delle prestazioni ottime.

\par Questi progetti rendono l'idea di come sia possibile effettivamente usare RDF per un approccio più aperto alla condivisione
dei dati, ma non si pongono il problema delle prestazione. La quantità di dati utilizzata raramente riesce
a raggiungere il milione di triple quando convertita in RDF pertanto non pone nessun particolare problema alla maggior parte
dei motori SPARQL più popolari. Ma per questo esistono diversi benchmark, analizzati nella prossima sezione.

\subsection{Benchmark esistenti}

Esiste un gran numero di insiemi di datasets e query che possiamo usare per misurare le prestazioni dei vari
triple store qui considerati, ma nessuno sembrerebbe rispecchiare in modo preciso e complete il carico di lavoro che si andrebbe 
ad incontrare se si utilizzasse RDF come formato generico di rappresentazione. Se pensiamo agli accessi che normalmente vengono
fatti su un file, la maggior parte sono letture di grandi porzioni dello stesso. Questo cambierebbe forse avendo a supporto
le query SPARQL ma dall'esperienza di 3DOnt il grande collo di bottiglia sono appunto le dimensioni degli enormi risultati e 
il poco vantaggio dato dall'indicizzazione classica nelle grandi scansioni. 

\par Il confronto tra triple store passa di solito per un piccolo insieme di benchmark sintetici: LUBM~\cite{guo2005lubm},
il Berlin SPARQL Benchmark~\cite{bizer2009bsbm}, SP\textsuperscript{2}Bench~\cite{schmidt2009sp2bench} e
WatDiv~\cite{aluc2014watdiv}. Sono strumenti maturi e utili per misurare i motori tra loro, ma il carico che costruiscono
ruota attorno alla selettività delle query e all'ordinamento dei join, non al volume dei risultati; WatDiv in particolare
fa variare la struttura delle query più di quanto faccia variare quante righe tornano. Nessuno di questi mette quindi sotto
pressione la parte che qui interessa, ovvero la scansione e la serializzazione di milioni di righe.

\par BowlognaBench~\cite{demartini2012bowlogna} ad esempio parte sì da una base di dati consistente (i registri accademici dell'Università di Bologna) ma
nel vasto numero di query analitiche definite la dimensione dei risultati è sempre molto ridotta. È quindi comunque interessante
ma incompleto per il nostro caso d'uso. Soltanto la quarta query avrebbe un insieme di risultati largo ma prendendo essa in 
considerazione dati privati degli studenti non è replicabile localmente.
\par Stesso vale per SSB-QB4OLAP~\cite{etcheverry2017cubes}, che riprende da SSB-QB~\cite{kampgen2013ssbqb} l'idea di tradurre
lo Star Schema Benchmark in SPARQL, e il cui volume dei risultati è soltanto una frazione del volume totale dei dati, nonostante
stiamo sempre parlando di query OLAP. É comunque interessante confrontare i risultati qui ottenuti con quelli di questi 
altri benchmark, si vanno a rendere chiare le differenze di scopo nelle implementazioni dei vari motori.
\par LDBC Semantic Publishing Benchmark~\cite{kotsev2016spb} invece si mostra interessante su due delle query definite. La quarta infatti permette
di limitare le dimensioni del risultato, scegliendo un valore di limite sufficientemente elevato si possono analizzare appunto
le prestazioni dei triple store in più scansioni complete dei dati. Stesso discorso per la seconda query nella categoria avanzata,
simile a quella appena citata ma non ha nessun limite nella dimensione dei risultati.
Il benchmark in realtà è molto più complesso di così e andrebbe a simulare il lavoro su una base dati condivisa con operazioni di
modifica e aggregazione concorrenti. Il nostro caso d'uso invece richiede soltanto un accesso sequenziale da parte di un solo utente,
per cui le tecnologie implementate per aumentare il parallelismo dei triple store non forniscono alcun guadagno.
\par Va citato inoltre Smart City RDF Benchmark~\cite{bellini2018smartcity}, che usa dati geospaziali proveniente dai sensori
della città di Firenze, che però
richiede complesse capacità di indicizzazione del testo e accede solo a ridotte porzioni temporali per ogni query, trattandosi
di dati storici urbani.


\section{3Dont}
\label{sec:3dont}

\subsection{Ambito}

La nuvola di punti è un tipo di rappresentazione molto diffuso per dati tridimensionali geospaziali. Questo formato viene usato
in ambiti che vanno dall'urbanistica ai beni culturali e alle scienze forestali. La maggior parte dei processi di scansione e 
acquisizione di ambienti tridimensionali produce come primo risultato una nuvola di punti, su cui possono essere poi eseguiti un
vasto numero di processi, dalla segmentazione delle istanze all'individuazione delle geometrie esistenti. Una singola nuvola di punti
può rappresentare un piccolo oggetto come un'intera città, e al variare della risoluzione e della dimensione può arrivare ad avere
centinaia di milioni di punti. 
\par Esistono diversi formati per la condivisione di nuvole di punti, come PCD (Point Cloud Data)~\cite{rusu2011pcl},
LAS (LASer)~\cite{asprs-las14} e LAZ (LASzip)~\cite{isenburg2013laszip},
che in pratica permettono di definire una serie di campi presenti per ogni punto e la lista di punti della nuvola. Questi formati
sono più o meno complessi, LAS ad esempio permette solo 11 formati di nuvole di punti diverse (come con o senza tempo GPS, colore, etc.)
anche se grazie a questa caratteristica LASzip (la sua evoluzione con compressione) permettere di comprimere file LAS senza perdita 
di dettaglio in modo molto efficace.

\subsection{Il framework}

3Dont~\cite{codiglione2024urban,codiglione2024heritage} è un framework basato su RDF che permette di modellare nuvole di punti
attraverso un'ontologia definita ma estensibile.
In fase di creazione del grafo classifica i punti e gli oggetti della nuvola in base all'ambito scelto (urbano o beni culturali)
secondo una struttura gerarchica, per poi eseguire una fase di ragionamento che materializza delle informazioni aggiuntive come la
correlazione spaziale, classificazione tassonomica, proprietà dei materiali e così via.
\par Il modello così creato è pronto per essere visualizzato ed esplorato, anche attraverso delle interrogazioni in linguaggio naturale,
che grazie a un LLM e un'analisi dell'ontologia vengono tradotte in linguaggio SPARQL ed eseguite~\cite{codiglione2025nl2sparql}. Questo permette anche a individui
senza una formazione specifica su nuvole di punti, ontologie o informatica (come ad esempio un pianificatore urbanistico) 
di interagire con i dati e supportare il proprio lavoro in autonomia.

\begin{figure}[htb]
  \centering
  \begin{subfigure}[b]{0.36\textwidth}
    \centering
    \input{images/tikz/gerarchia_mereologica.tex}
    \caption{Risoluzione mereologica: quanto scendere nell'annidamento.}
    \label{fig:mereologia}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.60\textwidth}
    \centering
    \input{images/tikz/gerarchia_tassonomica.tex}
    \caption{Risoluzione tassonomica: le classi, con \texttt{rdfs:subClassOf}.}
    \label{fig:tassonomia}
  \end{subfigure}
  \caption{Le due risoluzioni con cui 3Dont controlla la segmentazione, indipendenti l'una dall'altra. La prima scegle quanto scendere nell'annidamento parte-tutto degli oggetti che un punto costituisce, la seconda quanto generalizzare la classe così ottenuta. Che siano davvero indipendenti si legge confrontando i due pannelli: \texttt{Capital}, \texttt{Column} e \texttt{Facade}, che a sinistra sono livelli successivi, a destra sono classi sorelle sotto \texttt{base:Architectural\_Element}. Le classi intermedie come \texttt{base:Immovable\_Object} si raggiungono soltanto generalizzando lungo il secondo asse.}
  \label{fig:gerarchie}
\end{figure}

\subsection{Vantaggi}

Come spiegato nell'introduzione, uno dei vantaggi principali di RDF rispetto ai corrispondenti formati nativi è l'estensibilità.
3Dont specifica un'ontologia base con un vasto numero di elementi già definiti per i vari ambiti a cui si rivolge (classificazione
delle superfici erbose e della massa verde, componenti architettonici, arredo urbano etc.) ma non mira ad essere completa, un utente
potrebbe senza sforzo aggiungere nuove classi, ad esempio nuovi materiali con nuove caratteristiche, senza compromettere l'integrazione
con la strumentazione esistente.
\par Un secondo vantaggio lo troviamo nell'espressività che di base il framework riesce a dare. Se partiamo da formati come LAS che
permettono di avere solo un numero limitato di caratteristiche per ogni punto, con 3Dont il potenziale è infinito; non dobbiamo
preoccuparci di problemi come l'assegnazione di più etichette di classificazione a un singolo punto ma invece possiamo integrare i
dati con informazioni aggiuntive come le relazioni tra i punti, tra macro entità (insiemi di punti), o informazioni spaziotemporali.
\par Nessuna di queste ultime caratteristiche è supportata nei formati di nuvole di punti classiche e l'utilizzo di un solo formato
di supporto per questa vasta gamma di informazioni è senza dubbio un ulteriore vantaggio del framework.
\par Ma uno dei punti di forza maggiore di 3Dont è la possibilità di interrogare i dati con un linguaggio potente come SPARQL,
in questo modo possiamo accedere in modo casuale alle nuvole di punti, esprimere potenti filtri e condurre analisi numeriche
senza bisogno di iterare sull'interezza dei punti.

\begin{figure}[htb]
  \centering
  \input{images/tikz/punto_rdf.tex}
  \caption{Le triple che descrivono un singolo punto: una per coordinata, una per canale di colore e una per ogni oggetto che il punto costituisce, da cui si risale alla classe dell'oggetto. Il costo in triple per punto è quello che porta la nuvola più piccola qui analizzata da $1.065.725$ punti a $11.725.772$ triple (sezione~\ref{ssub:dataset_piccolo}); l'estensione dell'ontologia consiste nell'aggiungere triple a questo grafo, senza toccare quelli esistenti.}
  \label{fig:punto_rdf}
\end{figure}

\subsection{Visualizzatore}
\label{subsec:visualizer}

Il framework mette disposizione, oltre allo strumento di creazione dei grafi popolati dai punti, un visualizzatore per questi grafi.
Caricando l'ontologia popolata in uno dei vari formati disponibili per rdf (turtle, n-triples, rdf-xml etc.) permette di creare
un progetto persistente con un'interfaccia diretta alla nuvola di punti, che può essere esplorata astraendo sopra alle nozioni di
grafo e SPARQL, interagendo con i punti usando mouse, tastiera e interrogazioni in linguaggio naturale. 
\par Il visualizzatore permette di visualizzare fondamentalmente quattro categorie diverse di risultati alle interrogazioni:
\begin{itemize}
\item Query di selezione (figura~\ref{fig:vis_select}): vengono evidenziati i punti ritornati dalla query, come ad esempio quelli appartenenti a un dato
oggetto oppure tutti i punti di un certo materiale.
\item Query scalari (figura~\ref{fig:vis_scalar}): i punti vengono colorati secondo una scala colorata lineare in base a un valore che viene ritornato dalla
query, come ad esempio la temperatura in una data acquisizione oppure una certa caratteristica del materiale dei punti.
\item Query di classificazione (figure~\ref{fig:vis_semantic} e~\ref{fig:vis_instance}): tutti i punti appartenenti a un certo oggetto vengono colorati allo stesso modo, univocamente.
Si può così visualizzare la nuvola completamente segmentata, semanticamente o per istanza, a qualunque punto desiderato della scala
tassonomico o mereologica (figura~\ref{fig:gerarchie}): un singolo punto può essere contemporaneamente parte di un capitello, della colonna a cui appartiene il capitello,
della facciata a cui appartiene la colonna e l'edificio a cui appartiene la facciata; è disponibile quindi una segmentazione per
diversi livelli di dettaglio.
\item Query tabellari: il risultato non è visualizzato graficamente attraverso la nuvola di punti ma in una tabella a lato, è il caso
delle query che ritornano singoli valori numerici o comunque valori che non sono collegati a singoli punti della nuvola.
\end{itemize}

\begin{figure}[htb]
  \centering
  \begin{subfigure}[b]{0.45\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/select_object.jpg}
    \caption{Query di selezione: i punti restituiti sono evidenziati in rosso.}
    \label{fig:vis_select}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.45\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/scalar_result.jpg}
    \caption{Query scalare: il colore dei punti segue una scala lineare sul valore restituito.}
    \label{fig:vis_scalar}
  \end{subfigure}
  \\[1.5ex]
  \begin{subfigure}[b]{0.45\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/semantic_segmentation.jpg}
    \caption{Query di classificazione, un colore per classe tassonomica.}
    \label{fig:vis_semantic}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.45\textwidth}
    \centering
    \includegraphics[width=\textwidth]{images/instance_segmentation.jpg}
    \caption{Query di classificazione, un colore per istanza di oggetto.}
    \label{fig:vis_instance}
  \end{subfigure}
  \caption{Le modalità di visualizzazione dei risultati nel visualizzatore 3Dont, tutte sulla nuvola del tempio di Nettuno descritta nella sezione~\ref{ssub:dataset_medio2}. Le query tabellari non compaiono qui: il loro risultato è mostrato in una tabella a lato e non sulla nuvola di punti.}
  \label{fig:viewer}
\end{figure}

\subsection{Implementazione}

La sfida principale nell'implementazione di 3Dont è sicuramente il raggiungimento di prestazioni compatibili con l'interattività
di un'interfaccia grafica e di un applicativo destinato a un'utenza inesperta. 
Il software deve integrare un triple store, con cui sarà possibili importare e interagire con diversi datasets (indicati come 
diversi "progetti"). Qualunque interazione con l'interfaccia, ad esclusione dei cambiamenti di visuale, comporta un'interazione
con il dato sottostante, dalla renderizzazione iniziale della nuvola all'ispezione di un singolo punto.
Nuvole di punti di medie dimensione (meno di 5 milioni di punti) possono richiedere per la query iniziale, che domanda coordinate e 
colori di ogni punto, diversi minuti di esecuzione, ma si può scendere a meno di una decina di secondi con i giusti strumenti.
\par Il principale fattore determinante (oltre naturalmente all'hardware utilizzato, che però ovviamente non dipende dall'implementazione
del framework) è il motore di query utilizzato e la sua configurazione. Nello specifico viene impiegato QLever~\cite{bast2017qlever,bast2022qlever} 
con vocabolario compresso su disco e i prefissi dell'ontologia codificati. È stato scelto per la sua disponibilità sotto licenza
open-source, la possibilità di integrarlo nativamente senza l'utilizzo di layer di virtualizzazione quali docker, ma soprattutto
per la sua velocità, di cui si potrà avere un riscontro completo nel capitolo seguente.
 Viene quindi usata la versione integrata di QLever, disponibile come libreria c++, che permette di ridurre al minimo l'utilizzo di 
risorse dato dall'IPC (Inter Process Communication) e dalla deserializzazione dei risultati, 
che invece sarebbe necessaria usando SPARQL tramite l'interfaccia HTTP.
